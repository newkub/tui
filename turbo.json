{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "test": {
      "dependsOn": ["build"],
      "inputs": ["src/**/*.ts", "test/**/*.ts"]
    },
    "lint": {
      "inputs": ["src/**/*.ts", "**/*.ts", "**/*.js"]
    },
    "format": {
      "inputs": ["src/**/*.ts", "**/*.ts", "**/*.js"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}